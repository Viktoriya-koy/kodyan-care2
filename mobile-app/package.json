{
  "name": "kodyan-care-mobile",
  "version": "1.0.0",
  "description": "App móvil Kodyan Care",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Build completo'",
    "android": "npx cap run android",
    "android-build": "npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
